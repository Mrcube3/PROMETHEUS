[project]
name = "prometheus-agent"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "pydantic>=2.6",
  "httpx>=0.27",
  "eth-account>=0.11"
]

[tool.vercel]
entrypoint = "prometheus.api.app:app"
